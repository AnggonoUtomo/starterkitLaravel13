# 03.04 Folder Structure

> Canonical repository structure untuk Laravel Engineering Starter Kit dan project turunan.

## Document Information

| Item | Value |
|---|---|
| Version | 1.1 |
 | Status | Approved |
| Owner | Software Architect |
| Depends On | `02.04-SYSTEM-DESIGN.md`, `03.01-CODING-STANDARD.md`, `03.07-MODULES.md` |

## Revision History

| Version | Date | Description |
|---|---|---|
| 1.1 | 2026-07-31 | Metadata normalization for SAKAAI validation |
| Related | `03.05-GENERATOR-SPEC.md`, `03.08-STUB-SPEC.md`, `03.10-TEST-PLAN.md` |

## Repository Root

```text
project/
├── app/
├── bootstrap/
├── config/
├── database/
├── packages/
├── public/
├── resources/
├── routes/
├── storage/
├── tests/
├── docs/
├── composer.json
├── package.json
├── phpunit.xml
├── vite.config.ts
├── tsconfig.json
└── README.md
```

## Backend Application

```text
app/
├── Console/
├── Http/
│   ├── Controllers/
│   ├── Middleware/
│   └── Requests/
├── Models/
├── Modules/
│   └── Platform/
│       ├── AccessControl/
│       ├── UserManagement/
│       ├── AuditLog/
│       └── SystemSetting/
├── Providers/
└── Support/
```

`app/Models` hanya berisi model shared/application foundation seperti model
`User` dari starter kit. Business model wajib berada di module owner.

## Module Structure

```text
app/Modules/Platform/{Module}/
├── Application/
│   ├── Actions/
│   ├── DTO/
│   ├── Services/
│   └── Contracts/
├── Domain/
│   ├── Contracts/
│   ├── Entities/
│   ├── Events/
│   ├── Services/
│   └── ValueObjects/
├── Infrastructure/
│   ├── Models/
│   ├── Repositories/
│   ├── Observers/
│   └── Providers/
├── Presentation/
│   ├── Controllers/
│   ├── Requests/
│   └── Resources/
├── Database/
│   ├── Migrations/
│   ├── Factories/
│   └── Seeders/
├── Routes/
├── Tests/
│   ├── Unit/
│   ├── Feature/
│   └── Integration/
├── module.json
├── module.php
├── permissions.php
├── ServiceProvider.php
└── README.md
```

Setiap module memiliki communication layer sendiri melalui `Contracts`,
`Events`, DTO/mapping, presentation boundary, dan authorization policy. Module
tidak boleh mengimpor private implementation module lain.

## Initial Platform Modules

```text
app/Modules/Platform/
├── AccessControl/
├── UserManagement/
├── AuditLog/
└── SystemSetting/
```

## Reusable Framework Package

```text
packages/
└── StarterKit/
    ├── src/
    │   ├── Console/
    │   ├── Contracts/
    │   ├── Discovery/
    │   ├── Generator/
    │   ├── Lifecycle/
    │   ├── Registry/
    │   ├── Support/
    │   └── Testing/
    ├── config/
    ├── resources/
    │   ├── stubs/
    │   └── schemas/
    ├── tests/
    ├── composer.json
    └── README.md
```

`StarterKit` tidak boleh berisi business logic platform module. Package menjadi
owner generator, registry, discovery, lifecycle, console, contract, dan shared
framework service.

## Frontend

```text
resources/js/
├── components/
│   ├── ui/
│   └── shared/
├── layouts/
├── pages/
│   └── Platform/
│       ├── AccessControl/
│       │   ├── components/
│       │   ├── pages/
│       │   ├── schemas.ts
│       │   └── types.ts
│       ├── UserManagement/
│       │   ├── components/
│       │   ├── pages/
│       │   ├── schemas.ts
│       │   └── types.ts
│       ├── AuditLog/
│       │   ├── components/
│       │   ├── pages/
│       │   ├── schemas.ts
│       │   └── types.ts
│       └── SystemSetting/
│           ├── components/
│           ├── pages/
│           ├── schemas.ts
│           └── types.ts
├── hooks/
├── lib/
├── routes/
├── types/
└── app.tsx
```

Module component wajib berada di `pages/Platform/{Module}/components`. Global
shared component hanya berada di `components/ui` atau `components/shared`.

## Database and Routes

```text
database/
├── factories/
├── migrations/
└── seeders/

routes/
├── web.php
├── api.php
└── console.php
```

Module-owned migration, factory, seeder, dan route berada di module directory;
root directory hanya berisi registration/entry point jika diperlukan Laravel.

## Tests

```text
tests/
├── Unit/
├── Feature/
├── Integration/
├── Browser/
├── Architecture/
└── Support/
```

Module-local behavior diuji di `app/Modules/{...}/Tests`. Cross-module contract,
architecture, browser, dan application smoke test berada di root `tests`.

## Documentation

```text
docs/
├── 00-PROJECT/
├── 01-REQUIREMENTS/
├── 02-DESIGN/
├── 03-IMPLEMENTATION/
├── 04-DEPLOYMENT/
├── 05-DECISIONS/
├── 06-FRAMEWORK/
└── 07-KERNEL/
```

Root `README.md` dan `AGENTS.md` menjadi entry point context. File dokumentasi
mengikuti nomor folder dan nomor file yang telah ditetapkan.

## Public and Storage

```text
public/
├── build/
├── favicon.ico
└── assets/

storage/
├── app/
├── framework/
└── logs/
```

Upload/media memakai storage abstraction dan Spatie Media Library; jangan
menyimpan user upload sensitif langsung pada public path.

## Folder Ownership Rules

- Folder hanya memiliki satu owner yang jelas.
- Shared folder tidak boleh menjadi tempat business logic acak.
- Generator wajib menghasilkan struktur sesuai dokumen ini.
- Perubahan struktur membutuhkan update generator, coding standard, test, dan ADR
  bila mengubah boundary.
- Jangan membuat folder alternatif dengan nama yang sama tetapi ownership berbeda.

## Confirmed Structure Decisions

- Migration, factory, dan seeder tetap module-local pada
  `app/Modules/{Domain}/{Submodule}/Database/`.
- Permission identity module tetap module-local pada
  `app/Modules/{Domain}/{Submodule}/permissions.php`.
- Frontend page entry point berada pada
  `resources/js/pages/Platform/{Module}/pages/`.
- Root `database/` dan root `resources/js/pages/` hanya menjadi entry point atau
  foundation Laravel; business implementation tetap berada pada owner module.

## References

- `02.04-SYSTEM-DESIGN.md`
- `02.05-UI-UX-GUIDELINE.md`
- `03.01-CODING-STANDARD.md`
- `03.05-GENERATOR-SPEC.md`
- `03.07-MODULES.md`
- `03.10-TEST-PLAN.md`
